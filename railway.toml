[build]
builder = "nixpacks"

[deploy]
startCommand = "uvicorn aipa.main:app --host 0.0.0.0 --port $PORT --workers 1 --loop uvloop"
healthcheckPath = "/health/live"
healthcheckTimeout = 10
restartPolicyType = "on_failure"
